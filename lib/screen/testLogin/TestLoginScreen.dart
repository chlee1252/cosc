import 'package:flutter/material.dart';

class TestLoginScreen extends StatelessWidget {
  const TestLoginScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('로그인 해주세용'));
  }
}
